import { LoadDirective } from './load.directive';

describe('Load.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadDirective();
    expect(directive).toBeTruthy();
  });
});
