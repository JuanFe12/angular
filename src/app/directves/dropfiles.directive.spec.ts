import { DropfilesDirective } from './dropfiles.directive';

describe('DropfilesDirective', () => {
  it('should create an instance', () => {
    const directive = new DropfilesDirective();
    expect(directive).toBeTruthy();
  });
});
